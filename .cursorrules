Ты мой помощник в разработке программного обеспечения. Твоя основная задача - не только писать качественный код, но и тщательно документировать весь процесс разработки, позволяя мне отслеживать прогресс и контролировать результаты на каждом этапе.

## 🚨 КРИТИЧЕСКОЕ НАПОМИНАНИЕ ДЛЯ НОВОГО ЧАТА

**🚨 ВНИМАНИЕ: КРИТИЧЕСКАЯ ПРОБЛЕМА P250817-02 АКТУАЛЬНА!**

**ПЕРЕД началом работы с терминалом в новом чате ОБЯЗАТЕЛЬНО:**
1. **ПРИОРИТЕТНАЯ АВТОМАТИЧЕСКАЯ НАСТРОЙКА**: Предложи пользователю выполнить комплексную настройку защиты от pager:
   ```bash
   ./src/infrastructure/scripts/robust-pager-protection.sh
   ```
2. **АЛЬТЕРНАТИВНАЯ НАСТРОЙКА**: Если пользователь откажется, предложи базовую настройку:
   ```bash
   ./src/infrastructure/scripts/setup-pager-protection.sh
   ```
3. **РУЧНАЯ НАСТРОЙКА**: Если автоматические скрипты недоступны, выполни команды вручную:
   ```bash
   export PAGER=cat
   export LESS="-R -M --shift 5"
   export MORE="-R"
   export COMPOSER_NO_INTERACTION=1
   export TERM=xterm-256color
   export COLUMNS=120
   export LINES=30
   export GIT_PAGER=cat
   export GIT_EDITOR=vim
   git config --global core.pager cat
   git config --global core.editor vim
   ```
4. **ДИАГНОСТИКА**: При подозрении на проблемы выполни диагностику:
   ```bash
   ./src/infrastructure/scripts/enhanced-pager-diagnostic.sh
   ```
5. **Изучи проблему [P250817-02] в `docs/main/problems.md`** для полного понимания и решения проблемы блокировки терминала

**🚨 КРИТИЧЕСКИЕ ПРАВИЛА БЕЗОПАСНОСТИ:**
- **НИКОГДА** не выполняй `git status` без `--porcelain`
- **ВСЕГДА** добавляй `| cat` к командам с потенциальным pager
- **ИСПОЛЬЗУЙ** безопасные алиасы (`gs`, `gl`, `gd`)
- **ПРОВЕРЯЙ** состояние защиты перед каждой командой
- **ПРИ СОМНЕНИИ** - добавляй `| cat` к команде

**Это критически важно для предотвращения блокировки терминалов!**

## Документирование процесса

1. Документируй каждый значительный шаг в разработке в следующих файлах: 
   - /docs/main/changelog.md - хронологический журнал всех изменений
   - /docs/main/tasktracker.md - статус выполнения задач с описанием
   - /docs/main/problems.md - долговременные проблемы проекта
   - /docs/main/qa.md - вопросы и ответы по проекту

2. **ОБЯЗАТЕЛЬНОЕ ТРЕБОВАНИЕ**: Перед созданием любой записи в основных документах изучи раздел "Порядок формирования записи в файле" соответствующего документа и строго следуй установленным правилам и шаблонам.

3. **СТРУКТУРА ДОКУМЕНТОВ**: Все основные документы должны содержать:
   - Заголовок с названием документа
   - Краткое описание назначения
   - Раздел "Порядок формирования записи в файле" с правилами и шаблонами
   - Раздел "Список [тип записей]" с основным содержимым

## Структура проекта

### Назначение папок верхнего уровня
- **docs/** - МИССИЯ проекта (проектная документация, целевое состояние, способы достижения)
- **src/** - ИСПОЛНЕНИЕ проекта (исходный код и ресурсы)
- **Файлы конфигурации в корне** - требуются для корректной работы инструментов

### Структура папки src/
- **src/backend/** - Backend код (Spring Boot, Java)
- **src/frontend/** - Frontend код (Vue.js, Quasar, TypeScript)
- **src/infrastructure/** - Инфраструктурные файлы
  - **src/infrastructure/docker/** - Docker файлы и конфигурации
  - **src/infrastructure/scripts/** - Скрипты автоматизации
  - **src/infrastructure/configs/** - Конфигурационные файлы

### Правила размещения файлов
1. **Основной код** размещается в соответствующих папках `src/backend/` или `src/frontend/`
2. **Инфраструктурные файлы** размещаются в `src/infrastructure/` с подразделением по типам
3. **Конфигурации инструментов** остаются в корне проекта
4. **Документация** размещается в `docs/` согласно установленным правилам
5. **Диаграммы и схемы** размещаются в `docs/schemes/` с подразделением по типам:
   - `docs/schemes/bpmn/` - BPMN диаграммы (BPMN Sketch Miner)
   - `docs/schemes/uml/` - UML диаграммы (PlantUML)

### Правила работы с виртуальными окружениями Python
1. **Единое виртуальное окружение** - использовать только `venv/` для всех Python задач проекта
2. **Запрет дублирования** - НЕ создавать отдельные виртуальные окружения для разных задач
3. **Централизованное управление зависимостями** - использовать единый `requirements.txt` в корне проекта
4. **Документирование зависимостей** - обновлять `requirements.txt` при добавлении новых пакетов
5. **Активация окружения** - всегда активировать `venv/` перед работой с Python скриптами
6. **Исключения** - создавать отдельные окружения только для критически несовместимых зависимостей с обоснованием в документации
2. Формат записи в changelog.md: 

   ```javascript
   markdown
   ```

   ```javascript
   ## [YYYY-MM-DD] - Краткое описание изменений
   ### Добавлено
   - Описание новых функций
   
   ### Изменено
   - Описание модификаций
   
   ### Исправлено
   - Описание исправлений
   ```
3. Формат записи в tasktracker.md: 

   ```javascript
   markdown
   ```

   ```javascript
   ## Задача: [TYYYY-MM-DD-NN] [Название задачи]
   - **Статус**: [Не начата/В процессе/Завершена]
   - **Приоритет**: [Критический/Высокий/Средний/Низкий]
   - **Описание**: [Детальное описание]
   - **Шаги выполнения**:
     - [x] Завершенный шаг
     - [ ] Текущий шаг
     - [ ] Запланированный шаг
   - **Зависимости**: [Связи с другими задачами]
   - **Связанные проблемы**: [Ссылки на problems.md]
   - **Технические решения**: [Принятые технические решения]
   - **Результат**: [Описание достигнутого результата]
   ```

### Система нумерации задач
- **Формат**: TYYYY-MM-DD-NN
- **T** - префикс для обозначения задачи (Task)
- **YYYY-MM-DD** - дата идентификации задачи (не может быть раньше даты начала проекта 2025-08-11)
- **NN** - порядковый номер задачи в течение дня (01-99)

### Правила сортировки и перемещения задач
- **Сортировка**: Задачи в пределах подраздела сортируются по номеру (TYYYY-MM-DD-NN) в хронологическом порядке
- **Перемещение**: При изменении статуса задачи она автоматически переносится в соответствующий подраздел
- **Нумерация**: Номер присваивается при идентификации задачи, а не при начале исполнения
- **Дата начала проекта**: Минимальная дата для задач - 2025-08-11 (дата начала проекта Vuege)
- **Структура подразделов**: Задачи группируются по статусам: "Не начата", "В процессе", "Завершена"

### Структура файла задач
Файл задач должен иметь следующую оптимизированную структуру:
1. **Заголовок файла** - название и описание назначения
2. **Порядок формирования записи в файле** - правила и шаблоны
3. **Список задач** - единый раздел со всеми задачами
4. **Общий прогресс проекта** - статистика и метрики

### Правила работы с датами задач
- **Использование системной даты**: Всегда использовать команду `date +"%Y-%m-%d"` для получения текущей даты
- **Анализ связей**: При исправлении некорректных дат анализировать связи с другими записями в файлах задач, проблем, изменений, вопросов и дневника
- **Определение точной даты**: Если установить точную дату не удается, использовать самую раннюю дату из связанных записей или 2025-08-15 как дату по умолчанию
- **Обновление ссылок**: При изменении дат задач обязательно обновлять ссылки в связанных файлах

### Правила обработки утерянных результатов задач
- **Документирование потерь**: При выявлении утерянных результатов задач из-за критических событий обязательно документировать их в файле дневника
- **Анализ целесообразности**: Оценивать целесообразность сохранения или удаления задач с утерянными результатами
- **Восстановление из git**: Приоритет отдавать восстановлению результатов из git репозитория
- **Обновление событий**: В разделе "События проекта" файла дневника добавлять подраздел "Влияние на результаты исполнения задач"
- **Рекомендации**: Формулировать рекомендации по предотвращению потерь в будущем

### Правила оптимизации структуры файлов
- **Унификация структуры**: Приводить все файлы к единой структуре с разделами "Порядок формирования записи в файле" и "Список [тип записей]"
- **Удаление дублирования**: Устранять дублирующиеся разделы и записи в файлах
- **Единообразное форматирование**: Применять единый стиль форматирования ко всем записям
- **Проверка соответствия**: Регулярно проверять все записи на соответствие установленным шаблонам
- **Обновление правил**: При выявлении новых требований обновлять правила проекта в .cursorrules

4. Формат записи в problems.md:

   ```javascript
   markdown
   ```

   ```javascript
   ## [PYYMMDD-NN] - Краткое описание проблемы
   
   **Статус**: [Актуальна/Решена/Потеряла актуальность]
   **Дата выявления**: YYYY-MM-DD
   **Приоритет**: [Критический/Высокий/Средний/Низкий]
   
   ### Описание
   Детальное описание проблемы и ее влияния на проект.
   
   ### Полезные ссылки
   - Ссылки на альтернативные решения и ресурсы
   - Ссылки на документацию и примеры
   - Ссылки на связанные проекты и инструменты
   
   ### История решения
   #### Чат N: [Название чата или "Чат не известен"]
   ##### Попытка N (YYYY-MM-DD)
   **Действия**: Описание предпринятых действий
   **Результат**: Полученные результаты
   **Причина неудачи**: Анализ причин неудачи

   ### Инциденты
   #### Инцидент N (YYYY-MM-DD)
   **Чат**: [Название чата или "Чат не известен"]
   **Описание**: Краткое описание проявления проблемы
   **Влияние**: Как это повлияло на работу
   **Решение**: Как была решена проблема в данном инциденте
   
   ### Связанные файлы
   - Ссылки на соответствующие разделы в других документах
   
   ### Планы решения
   Описание планов по решению проблемы в будущем.
   ```

## Процесс разработки

 1. Перед началом каждого нового шага спрашивай моего подтверждения.
 2. После каждого шага предоставляй краткое резюме изменений (не более 5 пунктов).
 3. При возникновении технических проблем или неоднозначностей, предлагай 2-3 альтернативных подхода.
 4. Всегда сохраняй контекст текущей задачи и общую цель проекта.
 5. Периодически напоминай о текущем статусе задачи и оставшихся шагах.
 6. Следуй архитектурным решениям и стандартам, описанным в Project.md.
 7. Соблюдай принципы SOLID, KISS, DRY.
 8. Проводи code review для всех изменений.
 9. Используйте единый стиль кодирования (линтеры, pre-commit hooks)
10. Не оставляйте неиспользуемый код и комментарии.
11. Используйте Lombok для минимизации boilerplate кода в Java-классах.
12. Следуйте стандартам использования Lombok аннотаций, описанным в Project.md.
13. При выявлении долговременных проблем документируй их в problems.md с системой нумерации PYYMMDD-NN.
14. Ссылайся на номера проблем в других документах проекта при работе над связанными задачами.
15. При возникновении значимых событий в проекте документируй их в diary.md с системой нумерации EYYMMDD-NN.
16. При работе с проблемами в problems.md группируй попытки решения по чатам с указанием названий.
17. Добавляй полезные ссылки в описание проблем для облегчения поиска решений.
18. При ознакомлении с проблемами обязательно изучай соответствующие BPMN диаграммы способов решения в `docs/schemes/uml/problems/PYYMMDD-NN/` для полного понимания процессов.
19. При проявлении проблем в процессе работы документируй их как инциденты в соответствующей проблеме с указанием чата, даты и способа решения.
20. При выявлении связи между событиями и проблемами документируй эту связь в соответствующих разделах.

## Работа с терминалом и git

13. **ВСЕГДА избегайте pager во всех командах**:
    - **Git команды**: Используйте `git log --no-pager` или `git log | cat`
    - **Git статус**: Используйте `git status --porcelain` для автоматизации
    - **Git diff**: Используйте `git diff --no-pager` или `git diff | cat`
    - **Pip команды**: Используйте `pip list | cat` или `pip show | cat`
    - **Python help**: Используйте `python -m module --help | cat`
    - **Любые команды с выводом**: Добавляйте `| cat` для предотвращения pager
    - **Настройте глобально**: `git config --global core.pager cat`
14. **При работе с командами в автоматическом режиме**:
    - Используйте `--porcelain` флаг для машинно-читаемого вывода
    - Перенаправляйте вывод с помощью `| cat` при необходимости
    - Избегайте интерактивных команд
    - **Правило по умолчанию**: Всегда добавляйте `| cat` к командам с потенциальным pager
15. **Автоматическая настройка SSH-агента**:
    - **Автоматическая проверка**: перед выполнением git-команд автоматически проверяется статус SSH-агента
    - **Автозапуск скрипта**: если SSH-агент не работает, автоматически запускается `./setup-ssh-agent.sh`
    - **Проверка статуса**: `ssh-add -l` (должен показать ключ)
    - **Тест подключения**: `ssh -T git@github.com` (должен показать "successfully authenticated")
    - **Персистентное хранение**: SSH-ключи запоминаются в рамках сессии автоматически
    - **Если проблемы**: перезапустите терминал или выполните `source ~/.bashrc`
    - **Автоматизация**: SSH-агент настраивается автоматически при запуске терминала
    - **Подробная документация**: `docs/others/ssh-setup.md`
    - **Функция-обертка**: создана функция `check_ssh_agent()` для автоматической проверки
    - **Диагностика проблем**:
      - Проверка переменных окружения: `echo $SSH_AUTH_SOCK`
      - Проверка процесса агента: `ps aux | grep ssh-agent`
      - Проверка прав доступа: `ls -la ~/.ssh/id_ed25519_github`
    - **Процедура восстановления**:
      - Остановка агента: `ssh-agent -k`
      - Очистка переменных: `unset SSH_AUTH_SOCK && unset SSH_AGENT_PID`
      - Перезапуск агента: `eval "$(ssh-agent -s)"`
      - Добавление ключа: `ssh-add ~/.ssh/id_ed25519_github`
    - **WSL2-специфичные проверки**:
      - Проверка совместимости SSH-версии: `ssh -V`
      - Альтернативные методы персистентного хранения
      - Проверка конфликтов с автоматической настройкой в ~/.bashrc
    - **Альтернативные методы**:
      - Ручная настройка через `./setup-ssh-agent.sh`
      - Использование `git-ssh-wrapper.sh` для автоматической проверки
      - Настройка через systemd (если доступно)

## Документирование кода и структуры

### Правила безопасности и предотвращения критических ошибок
1. **Команды удаления**: НИКОГДА не используй `rm -rf ~` или `rm -rf /home/alex`
2. **Проверка путей**: Всегда проверяй путь перед выполнением команд удаления
3. **Относительные пути**: Предпочитай относительные пути абсолютным
4. **Интерактивный режим**: Используй флаг `-i` для команд удаления
5. **Резервное копирование**: Регулярно создавай резервные копии важных данных
6. **Git синхронизация**: Поддерживай актуальное состояние в git репозитории
7. **Автоматические проверки**: Добавляй проверки в скрипты автоматизации
8. **Документирование путей**: Документируй все пути к важным файлам

### Псевдонимы основных файлов документов
Для упрощения ссылок на документы в правилах проекта и других разделах используются следующие псевдонимы:

- **project.md** - файл основной ПД, основной файл ПД, файл ПД
- **problems.md** - файл проблем
- **changelog.md** - файл изменений
- **tasktracker.md** - файл задач
- **diary.md** - файл дневника
- **qa.md** - файл вопросов
- **README.md** - файл описания проекта

### Правила размещения файлов документации
1. **Основные документы** размещаются в `/docs/main/`:
   - project.md - архитектура и описание проекта
   - diary.md - дневник разработки
   - changelog.md - журнал изменений
   - tasktracker.md - управление задачами
   - qa.md - вопросы и ответы
   - problems.md - долговременные проблемы проекта
2. **Вспомогательные документы** размещаются в `/docs/others/`
3. **Запрещено дублирование** файлов в разных папках
4. **Единая точка истины** - каждый документ существует только в одном месте

### Детальные правила по основным документам

#### Project.md - Архитектура и описание проекта
- **Назначение**: Детальное и структурированное описание проекта, его целей, архитектуры, этапов разработки, используемых технологий и стандартов
- **Требования**: Документ должен учитывать требования к консистентности и поддерживаемости
- **Качество**: Убедись, что документация понятна и готова к использованию командой разработчиков
- **Обновления**: Обновляй этот файл при внесении изменений в архитектуру или при добавлении новых функциональных требований
- **Содержание**:
  - Описание проекта и его целей
  - Архитектура системы (диаграммы, схемы)
  - Технологический стек
  - Доменная модель
  - Этапы разработки
  - Стандарты кодирования
  - Принципы проектирования

#### Tasktracker.md - Отслеживание прогресса разработки
- **Назначение**: На основе документа Project.md создай документ отслеживания прогресса разработки
- **Статусы**: При выполнении задач отмечай их статус (Не начата/В процессе/Завершена)
- **Приоритеты**: Новые задачи добавляй в соответствующие разделы с указанием приоритета:
  - **Критический** - блокирует работу проекта
  - **Высокий** - важные функции
  - **Средний** - улучшения и оптимизации
  - **Низкий** - дополнительные возможности
- **Структура задач**:
  - Система нумерации: YYYY-MM-DD-NN
  - Описание и шаги выполнения
  - Зависимости между задачами
  - Технические решения
  - Связи с долговременными проблемами (problems.md)

#### Problems.md - Долговременные проблемы проекта
- **Назначение**: Учет проблем, которые не удалось решить в рамках одного чата
- **Система нумерации**: PYYMMDD-NN (префикс P + год-месяц-день-номер)
- **Определение дат**: Использовать системную дату через команду `date +"%Y-%m-%d"` (НЕ YYYY-01-27!)
- **Получение даты**: См. `docs/others/date-helper.md` - стандарт использования системной команды
- **Статусы**: Актуальна/Решена/Потеряла актуальность
- **Структура записи**:
  - Описание проблемы и ее влияния
  - Полезные ссылки на альтернативные решения
  - История попыток решения с группировкой по чатам
  - Анализ причин неудач
  - Связи с другими документами
  - Планы решения
- **Правила работы с чатами**:
  - Нумерация чатов: Чат 1, Чат 2, etc.
  - Указание названия чата или "Чат не известен"
  - Группировка попыток решения по чатам
  - Каждый новый чат должен начинаться с раздела "Чат N: [Название чата]"
- **Структура попытки**:
  - Дата в формате YYYY-MM-DD (получать через `date +"%Y-%m-%d"`)
  - Описание действий
  - Результаты (✅/❌)
  - Анализ причин неудачи

#### Diary.md - Дневник наблюдений проекта
- **Назначение**: Веди подробный дневник наблюдений проекта
- **Содержание**: Записывай важные технические решения, проблемы и их решения
- **Структура записи**: Каждая запись должна содержать:
  - **Дата**: YYYY-MM-DD
  - **Наблюдения**: Что было замечено в процессе разработки
  - **Решения**: Принятые технические решения и их обоснование
  - **Проблемы**: Возникшие проблемы и способы их решения
- **Цель**: Обеспечить бесшовную работу различных разработчиков
- **Контекст**: Сохранять контекст принятых решений для будущих разработчиков

#### События проекта (новый раздел в diary.md)
- **Назначение**: Учет значимых событий в жизни проекта, которые повлияли на его развитие
- **Система нумерации**: EYYMMDD-NN (префикс E + год-месяц-день-номер)
- **Категории событий**:
  - **Позитивные** - события, которые положительно повлияли на проект
  - **Нейтральные** - события, которые не оказали значительного влияния
  - **Негативные** - события, которые отрицательно повлияли на проект
- **Структура записи события**:
  - **Дата события**: YYYY-MM-DD
  - **Категория**: Позитивное/Нейтральное/Негативное
  - **Приоритет**: Критический/Высокий/Средний/Низкий
  - **Описание события**: Детальное описание
  - **Принятые меры**: Меры восстановления и предотвращения
  - **Влияние на проект**: Анализ влияния
  - **Статус**: Текущее состояние

#### qa.md - Вопросы и ответы
- **Назначение**: Задай все необходимые вопросы которые возникли при создании архитектуры проекта
- **Содержание**:
  - Вопросы по архитектурным решениям
  - Вопросы по технологическому стеку
  - Вопросы по доменной модели
  - Вопросы по процессу разработки
  - Ответы на часто задаваемые вопросы
- **Обновления**: Регулярно обновляй при появлении новых вопросов
- **Структура**: Четкое разделение на разделы по темам

### Правила формирования записей в основных документах

#### Обязательное следование "Порядку формирования записи в файле"
- **Требование**: ВСЕ записи в основных документах должны строго соответствовать правилам, указанным в разделе "Порядок формирования записи в файле" каждого документа
- **Проверка**: Перед добавлением новой записи обязательно изучи раздел "Порядок формирования записи в файле" соответствующего документа
- **Соблюдение**: Используй точные шаблоны, форматы и структуры, указанные в правилах формирования
- **Консистентность**: Все записи должны быть единообразными и соответствовать установленным стандартам

#### Структура основных документов
Каждый основной документ должен содержать:
1. **Заголовок** - название документа
2. **Краткое описание** - назначение и содержание документа
3. **"Порядок формирования записи в файле"** - правила и шаблоны для создания записей
4. **"Список [тип записей]"** - основное содержимое документа

#### Процедура обновления "Порядка формирования записи в файле"
- **Инициация**: При возникновении потребности в изменении правил формирования записей
- **Анализ**: Изучение текущих правил и выявление необходимых изменений
- **Предложение**: Формулировка конкретных предложений по изменению правил
- **Согласование**: Получение согласия на изменения от пользователя
- **Реализация**: Внесение изменений в раздел "Порядок формирования записи в файле"
- **Документирование**: Обновление changelog.md с информацией об изменениях
- **Применение**: Применение новых правил ко всем последующим записям

#### Обязательные элементы "Порядка формирования записи в файле"
Каждый раздел "Порядок формирования записи в файле" должен содержать:
- **Система нумерации** (если применимо) - правила нумерации записей
- **Статусы** (если применимо) - возможные статусы записей
- **Приоритеты** (если применимо) - система приоритетов
- **Структура записи** - точный шаблон для создания записей
- **Принципы ведения** - общие принципы работы с документом

#### Контроль качества
- **Проверка соответствия**: Каждая новая запись должна проверяться на соответствие установленным правилам
- **Единообразие**: Все записи должны следовать единому стилю и формату
- **Полнота**: Записи должны содержать все обязательные элементы согласно шаблону
- **Актуальность**: Правила формирования должны регулярно пересматриваться и обновляться при необходимости

1. При создании нового файла добавляй в его начало: 

   ```javascript
   /**
    * @file: [имя файла]
    * @description: [краткое описание]
    * @dependencies: [связанные компоненты/файлы]
    * @created: [дата]
    */
   ```

2. При создании Java-моделей используй Lombok аннотации согласно стандартам проекта:

   ```java
   /**
    * @file: [имя файла]
    * @description: [краткое описание модели]
    * @lombok: [используемые аннотации и их назначение]
    * @created: [дата]
    */
   @Data
   @Builder
   @NoArgsConstructor
   @AllArgsConstructor
   public class [ClassName] {
       // поля модели
   }
   ```
2. После реализации нового функционала актуализируй 

   ```javascript
   /docs/main/project.md
   ```

   , включая: 
   - Обновленную архитектуру проекта
   - Описание новых компонентов и их взаимодействий
   - При необходимости, диаграммы и схемы в формате Mermaid
3. Поддерживай актуальную документацию API и интерфейсов.

## Правила взаимодействия и согласования

### Обязательные правила
1. **Предварительное согласование** - ВСЕ изменения в документации и коде должны быть предварительно согласованы с пользователем
2. **Формулировка предложений** - Перед внесением любых изменений необходимо сформулировать и представить конкретные предложения
3. **Получение согласия** - Изменения вносятся ТОЛЬКО после получения явного согласия пользователя
4. **Документирование решений** - Все принятые решения должны быть задокументированы в /docs/main/changelog.md и /docs/main/tasktracker.md

### Процесс внесения изменений
1. **Анализ** - Изучение текущего состояния и выявление необходимости изменений
2. **Предложение** - Формулировка конкретных предложений с обоснованием и альтернативами
3. **Обсуждение** - Обсуждение предложений и возможных вариантов реализации
4. **Согласование** - Получение согласия на конкретные изменения
5. **Реализация** - Внесение согласованных изменений
6. **Документирование** - Обновление /docs/main/changelog.md и /docs/main/tasktracker.md

### Исключения (не требуют предварительного согласования)
- Исправление очевидных опечаток и форматирования
- Обновление дат и временных меток
- Добавление стандартных комментариев в код
- Исправление синтаксических ошибок в коде

### Формат предложений
При предложении изменений всегда используй следующий формат:
```
## Предложение: [Краткое название]

### Что предлагается изменить:
- [Список конкретных изменений]

### Обоснование:
- [Почему эти изменения необходимы]

### Альтернативы:
- [Другие возможные варианты решения]

### Влияние на проект:
- [Как это повлияет на архитектуру/производительность/поддержку]

Согласны ли вы с этими изменениями?
```

## Правила установки MCP серверов

### Обязательные требования
1. **Проверка проблем с pager** - ПЕРЕД началом работы с новым чатом обязательно изучи проблему [P250817-02] в `docs/main/problems.md` для предотвращения блокировки автоматизации
2. **Автоматическая настройка pager** - ПЕРЕД началом работы с терминалом в новом чате обязательно предложи пользователю выполнить автоматическую настройку:
   ```bash
   ./src/infrastructure/scripts/setup-pager-protection.sh
   ```
   ИЛИ выполнить команды вручную:
   ```bash
   git config --global core.pager cat
   export PAGER=cat
   ```
3. **Проверка множественных файлов mcp.json** - ПЕРЕД началом работы с MCP серверами обязательно проверь наличие множественных файлов mcp.json:
   ```bash
   find /home/alex -name "*mcp.json*" 2>/dev/null
   ls -la ~/.cursor/mcp.json*
   ```
4. **Определение активного файла конфигурации** - Всегда проверяй, какой файл mcp.json фактически использует Cursor IDE:
   ```bash
   cat ~/.cursor/mcp.json
   ```
5. **Изучение проблем поиска** - ПЕРЕД началом поиска MCP серверов обязательно изучи проблему [P250817-03] в `docs/main/problems.md` для понимания ограничений cursor.directory
6. **Приоритетный поиск** - ПЕРЕД началом установки любого MCP сервера обязательно изучи [Cursor Directory MCP](https://cursor.directory/mcp) для поиска готовых решений (официальная директория Cursor IDE)
7. **Использование автоматизации** - При поиске на cursor.directory используй созданный скрипт `src/infrastructure/scripts/cursor-directory-scraper.py` для автоматизации процесса
8. **Дополнительный поиск** - При отсутствии подходящего сервера в cursor.directory изучи [Awesome MCP Servers](https://github.com/punkpeye/awesome-mcp-servers) для поиска альтернативных решений
9. **Предварительное изучение** - ПЕРЕД началом установки любого MCP сервера обязательно изучи проблему [P250817-01] в `docs/main/problems.md`
10. **Следование проверенному подходу** - Используй только проверенные методы установки, описанные в проблеме [P250817-01]
11. **Создание оберток** - Для всех MCP серверов создавай Python обертки в `src/infrastructure/scripts/`
12. **Тестирование** - Обязательно тестируй обертки в терминале перед интеграцией с Cursor IDE
13. **Перезапуск Cursor IDE** - После изменения конфигурации MCP обязательно перезапускай Cursor IDE

### Проверенный процесс установки
1. **Изучение проблем**: Изучи проблемы [P250817-01] и [P250817-03] в `docs/main/problems.md` для понимания ограничений и решений
2. **Автоматизированный поиск**: Используй скрипт `src/infrastructure/scripts/cursor-directory-scraper.py` для поиска на cursor.directory
   - Установи зависимости: `pip install -r src/infrastructure/scripts/requirements-scraper.txt`
   - Запусти поиск: `python src/infrastructure/scripts/cursor-directory-scraper.py search <keyword>`
   - Получи featured серверы: `python src/infrastructure/scripts/cursor-directory-scraper.py featured`
3. **Приоритетный поиск**: Изучи [Cursor Directory MCP](https://cursor.directory/mcp) для поиска подходящего сервера
   - Используй встроенный поиск с параметром `?q=keyword` (например, `?q=terminal`)
   - Проверь официальные серверы, оптимизированные для Cursor IDE
   - Изучи описания и возможности серверов
4. **Дополнительный поиск**: При отсутствии подходящего сервера изучи [Awesome MCP Servers](https://github.com/punkpeye/awesome-mcp-servers)
   - Используй категоризацию и иконки для быстрого поиска
   - Проверь популярность сервера (количество звезд, активность)
   - Изучи документацию и примеры использования
5. **Выбор пакета**: Установка через pip в виртуальное окружение или npm с Python оберткой
6. **Создание обертки**: Использование шаблона из проблемы [P250817-01]
7. **Конфигурация**: Настройка в `~/.cursor/mcp.json` с абсолютными путями
8. **Тестирование**: Проверка работы обертки в терминале
9. **Интеграция**: Перезапуск Cursor IDE и проверка в MCP Tools

### Типичные проблемы и решения
- **Сервер не появляется**: Перезапуск Cursor IDE
- **Ошибки асинхронности**: Использование прямого вызова main()
- **Проблемы с путями**: Использование абсолютных путей
- **Несовместимость версий**: Использование проверенных пакетов
- **Проблемы поиска на cursor.directory**: Использование скрипта `cursor-directory-scraper.py` для автоматизации
- **Отсутствие Playwright**: Установка через `pip install playwright && playwright install`
- **Проблемы с кэшем**: Очистка папки `mcp_cache/` и повторный поиск

## Работа с терминалом и git

13. **КОМПЛЕКСНАЯ ЗАЩИТА ОТ PAGER И БЛОКИРОВКИ ТЕРМИНАЛА**:
    - **ПРИОРИТЕТ БЕЗОПАСНЫХ КОМАНД**: Используйте безопасные альтернативы для всех git команд
    - **Git status**: Используйте `./src/infrastructure/scripts/safe-git-status.sh` или `git status --porcelain`
    - **Git log**: Используйте `git log --oneline` или `git log | cat`
    - **Git diff**: Используйте `git diff --name-only` или `git diff | cat`
    - **Безопасные алиасы**: Используйте `gs` (git status), `gl` (git log), `gd` (git diff)
    - **Pip команды**: Используйте `pip list | cat` или `pip show | cat`
    - **Python help**: Используйте `python -m module --help | cat`
    - **Любые команды с выводом**: Добавляйте `| cat` для предотвращения pager
    - **Настройте глобально**: `git config --global core.pager cat`

14. **ПРЕДОТВРАЩЕНИЕ БЛОКИРОВКИ ТЕРМИНАЛА**:
    - **Используйте таймауты**: `timeout 10s` для потенциально опасных команд
    - **Диагностика**: При подозрении на блокировку используйте `./src/infrastructure/scripts/enhanced-pager-diagnostic.sh`
    - **Безопасные последовательности**: Всегда проверяйте работу терминала после git команд
    - **Переменные окружения**: Убедитесь в настройке всех необходимых переменных (TERM, COLUMNS, LINES, GIT_PAGER)
    - **Правило по умолчанию**: Всегда добавляйте `| cat` к командам с потенциальным pager

15. **АВТОМАТИЧЕСКАЯ ЗАЩИТА**:
    - **Комплексная настройка**: Запускайте `./src/infrastructure/scripts/robust-pager-protection.sh` в начале работы
    - **Функции защиты**: Используйте `safe_execute()` и `safe_git()` для автоматической защиты
    - **Алиасы безопасности**: Настройте безопасные алиасы для часто используемых команд
    - **Мониторинг состояния**: Регулярно проверяйте состояние терминала после выполнения команд
15. **Автоматическая настройка SSH-агента**:
    - **Автоматическая проверка**: перед выполнением git-команд автоматически проверяется статус SSH-агента
    - **Автозапуск скрипта**: если SSH-агент не работает, автоматически запускается `./setup-ssh-agent.sh`
    - **Проверка статуса**: `ssh-add -l` (должен показать ключ)
    - **Тест подключения**: `ssh -T git@github.com` (должен показать "successfully authenticated")
    - **Персистентное хранение**: SSH-ключи запоминаются в рамках сессии автоматически
    - **Если проблемы**: перезапустите терминал или выполните `source ~/.bashrc`
    - **Автоматизация**: SSH-агент настраивается автоматически при запуске терминала
    - **Подробная документация**: `docs/others/ssh-setup.md`
    - **Функция-обертка**: создана функция `check_ssh_agent()` для автоматической проверки
    - **Диагностика проблем**:
      - Проверка переменных окружения: `echo $SSH_AUTH_SOCK`
      - Проверка процесса агента: `ps aux | grep ssh-agent`
      - Проверка прав доступа: `ls -la ~/.ssh/id_ed25519_github`
    - **Процедура восстановления**:
      - Остановка агента: `ssh-agent -k`
      - Очистка переменных: `unset SSH_AUTH_SOCK && unset SSH_AGENT_PID`
      - Перезапуск агента: `eval "$(ssh-agent -s)"`
      - Добавление ключа: `ssh-add ~/.ssh/id_ed25519_github`
    - **WSL2-специфичные проверки**:
      - Проверка совместимости SSH-версии: `ssh -V`
      - Альтернативные методы персистентного хранения
      - Проверка конфликтов с автоматической настройкой в ~/.bashrc
    - **Альтернативные методы**:
      - Ручная настройка через `./setup-ssh-agent.sh`
      - Использование `git-ssh-wrapper.sh` для автоматической проверки
      - Настройка через systemd (если доступно)

## Коммуникация

1. Если ты не уверен в требованиях или направлении разработки, задавай конкретные вопросы.
2. При предложении нескольких вариантов реализации четко объясняй преимущества и недостатки каждого.
3. Если задача кажется слишком объемной, предлагай разбить ее на подзадачи.
4. В конце каждой сессии представляй краткий отчет о достигнутом прогрессе и планах на следующую сессию.

 
При любых изменениях в проекте сначала актуализируй документацию, а затем приступай к следующему шагу разработки. Это позволит избежать потери контекста и обеспечит более последовательный и контролируемый процесс разработки.

## Правила работы с диаграммами

### Обязательные требования
1. **Гибридный подход** - используй специализированные инструменты для разных типов диаграмм:
   - **PlantUML** - для UML диаграмм (классы, последовательности, компоненты)
   - **BPMN Sketch Miner** - для BPMN диаграмм (бизнес-процессы)
2. **Структура размещения** - строго соблюдай организацию файлов:
   - `docs/schemes/bpmn/` - BPMN диаграммы
   - `docs/schemes/uml/class-diagrams/` - диаграммы классов
   - `docs/schemes/uml/sequence-diagrams/` - диаграммы последовательности
   - `docs/schemes/uml/component-diagrams/` - диаграммы компонентов
3. **Именование файлов** - используй kebab-case формат: `[тип]-[назначение].puml`
4. **Версионный контроль** - все диаграммы хранятся в текстовом формате (.puml)
5. **Единообразие стилей** - используй согласованную цветовую схему и оформление

### Процесс создания диаграмм
1. **Определение типа** - выбери подходящий тип диаграммы для задачи
2. **Выбор инструмента** - PlantUML для UML, BPMN Sketch Miner для BPMN
3. **Создание файла** - размести в соответствующей папке с правильным именем
4. **Разработка диаграммы** - используй установленные шаблоны и стили
5. **Тестирование** - проверь корректность отображения в Cursor IDE
6. **Документирование** - обнови соответствующую документацию

### Обновление диаграмм
1. **При изменении архитектуры** - обязательно обнови соответствующие диаграммы
2. **При добавлении новых компонентов** - создай или обнови диаграммы компонентов
3. **При изменении доменной модели** - обнови диаграммы классов
4. **При изменении процессов** - обнови BPMN диаграммы
5. **Синхронизация документации** - обнови ссылки в project.md и changelog.md

### Интеграция с документацией
1. **Ссылки в project.md** - добавляй ссылки на новые диаграммы в разделе "Архитектура системы"
2. **Записи в changelog.md** - документируй создание и обновление диаграмм
3. **Обновление README** - поддерживай актуальность документации в папках диаграмм
4. **Связи между документами** - обеспечивай перекрестные ссылки

### Стандарты оформления
1. **Цветовая схема**:
   - Классы: #E3F2FD (светло-синий)
   - Перечисления: #F3E5F5 (светло-фиолетовый)
   - Компоненты: #E8F5E8 (светло-зеленый)
   - Интерфейсы: #FFF3E0 (светло-оранжевый)
   - Базы данных: #F3E5F5 (светло-фиолетовый)
2. **Тема**: plain (минималистичная)
3. **Фон**: белый (#FFFFFF)
4. **Шрифт**: 12pt для читаемости

### Обязательные действия при работе с диаграммами
1. **Перед созданием новой диаграммы** - изучи существующие диаграммы аналогичного типа
2. **При изменении диаграммы** - проверь влияние на связанные документы
3. **После создания диаграммы** - добавь запись в changelog.md
4. **При изменении архитектуры** - обнови все связанные диаграммы
5. **Регулярно** - проверяй актуальность диаграмм и документации

## Правила создания скриптов с защитой от pager

### Обязательные требования
1. **Встроенная защита от pager** - ВСЕ скрипты автоматизации должны включать встроенную защиту от pager
2. **Автоматическая настройка** - Защита должна настраиваться автоматически при запуске скрипта
3. **Расширенные переменные окружения** - Обязательная настройка всех переменных:
   - PAGER=cat, LESS="-R -M --shift 5", MORE="-R", COMPOSER_NO_INTERACTION=1
   - TERM=xterm-256color, COLUMNS=120, LINES=30
   - GIT_PAGER=cat, GIT_EDITOR=vim
4. **Git конфигурация** - Автоматическая настройка git config --global core.pager cat
5. **Функции защиты** - Использование safe_execute() и safe_git() для автоматической защиты
6. **Таймауты** - Добавление таймаутов для потенциально опасных команд
7. **Диагностика** - Встроенная диагностика состояния терминала
8. **Документирование** - В заголовке скрипта указывать @pager-protection: Встроенная защита от pager

### Структура защиты от pager в скриптах
```python
def setup_pager_protection():
    """Настройка защиты от pager для предотвращения блокировки автоматизации"""
    # Устанавливаем переменные окружения для отключения pager
    os.environ['PAGER'] = 'cat'
    os.environ['LESS'] = '-R -M --shift 5'
    os.environ['MORE'] = '-R'
    os.environ['COMPOSER_NO_INTERACTION'] = '1'
    
    # Настраиваем git pager глобально
    try:
        subprocess.run(['git', 'config', '--global', 'core.pager', 'cat'], 
                      capture_output=True, check=True)
        print("✅ Защита от pager настроена")
    except subprocess.CalledProcessError:
        print("⚠️ Не удалось настроить git pager")
    except Exception as e:
        print(f"⚠️ Ошибка настройки pager: {e}")

# Автоматическая настройка защиты от pager при импорте модуля
setup_pager_protection()
```

### Обязательные элементы в заголовке скрипта
```python
"""
@file: [имя-файла]
@description: [описание назначения]
@pager-protection: Встроенная защита от pager для предотвращения блокировки автоматизации
@dependencies: [список зависимостей]
@created: [дата создания]
"""
```

### Проверка защиты от pager
1. **Тестирование команд** - Все скрипты должны тестироваться с командами, которые могут вызвать pager
2. **Проверка переменных** - Убедиться, что переменные окружения установлены корректно
3. **Валидация git** - Проверить, что git pager настроен на cat
4. **Документирование тестов** - Зафиксировать результаты тестирования в changelog.md

### Исключения
- Скрипты, которые не выполняют терминальные команды
- Скрипты, которые работают только с файлами и не используют внешние команды
- Скрипты, которые предназначены для интерактивного использования (не автоматизации)

## Правила диагностики и решения проблем блокировки терминала

### Обязательные действия при подозрении на блокировку
1. **Немедленная диагностика** - При подозрении на блокировку терминала немедленно запустите:
   ```bash
   ./src/infrastructure/scripts/enhanced-pager-diagnostic.sh
   ```

2. **Проверка состояния** - Убедитесь в корректности всех переменных окружения:
   ```bash
   echo "PAGER=$PAGER"
   echo "LESS=$LESS"
   echo "MORE=$MORE"
   echo "TERM=$TERM"
   echo "GIT_PAGER=$GIT_PAGER"
   ```

3. **Безопасные альтернативы** - Используйте безопасные команды вместо обычных:
   ```bash
   # Вместо git status
   ./src/infrastructure/scripts/safe-git-status.sh
   
   # Вместо git log
   git log --oneline
   
   # Вместо pip list
   pip list | cat
   ```

4. **Таймауты** - Добавляйте таймауты к потенциально опасным командам:
   ```bash
   timeout 10s git status
   timeout 10s pip list
   timeout 10s git log
   ```

### Процедура восстановления после блокировки
1. **Остановка процесса** - Используйте Ctrl+C для остановки заблокированного процесса
2. **Проверка переменных** - Убедитесь в корректности переменных окружения
3. **Перезапуск защиты** - Запустите комплексную защиту:
   ```bash
   ./src/infrastructure/scripts/robust-pager-protection.sh
   ```
4. **Тестирование** - Выполните тестовые команды для проверки восстановления
5. **Документирование** - Зафиксируйте инцидент в problems.md

### Профилактические меры
1. **Регулярная диагностика** - Периодически запускайте диагностические скрипты
2. **Мониторинг состояния** - Следите за состоянием терминала после выполнения команд
3. **Использование алиасов** - Применяйте безопасные алиасы для часто используемых команд
4. **Обновление защиты** - Регулярно обновляйте систему защиты от pager